function solution(K: number, A: number[]): number {
  let left = 0;
  let right = 0;
  let count = 0;
  const maxLimit = 1_000_000_000;
  let N = A.length;

  for (let i = 0; i < N; i++) {

    while(right < N){
      if
    }


  }

  return count;
}